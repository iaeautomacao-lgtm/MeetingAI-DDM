{
  "name": "meeting-ddm-mobile",
  "version": "1.0.0",
  "private": true,
  "description": "App Android (Capacitor) do painel Meeting DDM / Acordito",
  "scripts": {
    "sync": "cap sync android",
    "open": "cap open android",
    "build:apk": "cd android && gradlew.bat assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/core": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5",
    "@capacitor/cli": "^7.0.0"
  }
}
